# 桃花台コードクラブ ウェブサイト 運用方法

このサイトは静的な HTML / CSS / JavaScript で作られています（フレームワーク不使用）。
本書は日常的な更新作業の手順書です。Claude Code 向けの開発ガイドは `CLAUDE.ja.md` / `CLAUDE.md` を参照してください。

## ファイル構成

```
index.html      トップページ
news.html       お知らせ一覧
venue.html      開催場所・アクセス
contact.html    お問い合わせ・参加申込みフォーム
mentor.html     メンター募集
style.css       デザイン（通常は編集不要）
script.js       動作スクリプト（アニメーション・季節演出・フォーム。通常は編集不要）
events.js       ★ 開催予定の管理ファイル（定期的に更新）
i18n.js         多言語ランタイム（言語切替・適用のみ。テキストは含まない）
i18n/           ★ 言語別 UI テキスト（ja / en / pt / vi / es / zh / id の *.json）
logo.svg        ロゴ
favicon.svg           トップページ用アイコン
favicon-news.svg      お知らせページ用アイコン
favicon-venue.svg     開催場所ページ用アイコン
favicon-contact.svg   お問い合わせページ用アイコン
favicon-mentor.svg    メンター募集ページ用アイコン
運用方法.md     この文書
README.md       プロジェクト概要
CLAUDE.md / CLAUDE.ja.md   Claude Code 向け開発ガイド（英語 / 日本語）

scripts/
  check-i18n.py       多言語整合性チェックスクリプト
  hook-check-i18n.sh  Claude Code 用の自動チェックフック
.claude/
  settings.json       Claude Code プロジェクト設定（フック定義）
```

> **重要：UI テキストの置き場所が変わりました**
> 翻訳対象テキストは `i18n.js` ではなく、言語ごとの **`i18n/<言語>.json`** に格納されています。
> `i18n.js` は言語の切り替えと適用だけを担うランタイムで、テキストは含みません。

---

## 1. 開催予定を更新する（最も頻度が高い作業）

`events.js` を開いて `EVENTS` 配列を編集します。
サイト全体の「次回開催」情報は、すべてここを見て自動更新されます。

### 日程が決まったとき

`tbd` を `false` にして、`date` を設定します。
**曜日・季節アイコン（seasonEmoji）・「○月」表記（monthLabel）は日付から自動計算**されるので入力不要です。

```js
// 変更前
tbd:        true,
date:       null,

// 変更後（例: 2026年8月30日）
tbd:        false,
date:       '2026-08-30',   // YYYY-MM-DD（曜日・季節は自動）
```

### connpass の URL が決まったとき

`connpassUrl` の `null` を実際の URL に変更します。

```js
// 変更前
connpassUrl: null,
// 変更後
connpassUrl: 'https://connpass.com/event/1234567/',
```

URL を設定すると、各ページの「申込受付前」ボタンが自動的に「connpass で申込む」ボタンに切り替わります。

### 次の回を追加するとき

`EVENTS` 配列の先頭（コメント行の直下）に新しい項目を追加します。前回の項目は残しておいても問題ありません。

```js
{
  id:          1,
  label:       '第1回',                 // 回の名称（任意。無ければ「○月の桃花台コードクラブ」）
  kind:        'regular',               // 'prep'（おとな向け準備会）/ 'regular'（通常回）
  tbd:         false,                   // 日程未定なら true（date は null のまま）
  date:        '2026-09-27',            // YYYY-MM-DD（曜日・季節アイコン・○月表記は自動）
  startTime:   '13:30',
  endTime:     '15:30',
  venue:       '小牧市東部市民センター2F 学習室',
  connpassUrl: 'https://tokadai-code-club.connpass.com/event/xxxxxx/',
  // ↓ 任意項目（あれば次回開催カード等に表示、無ければ自動で省略）
  deadline:    '2026-09-26',            // 申込締切日
  deadlineTime:'17:00',
  audience:    'ニンジャ（小学生〜高校生）',  // 対象
  capacity:    '10名',                  // 定員
  fee:         '無料',                  // 参加費
  summary:     '',                      // 一言紹介
  notes:       '',
},
```

> `dayOfWeek` / `seasonEmoji` / `monthLabel` は通常書きません（`date` から自動）。
> 特別に固定したいときだけ明示すると、その値が優先されます。

`seasonEmoji`（自動計算）の対応：

| 季節 | 月 | 絵文字 |
|------|------|------|
| 春 | 3〜5月 | 🌸 |
| 夏 | 6〜8月 | 🌿 |
| 秋 | 9〜11月 | 🍂 |
| 冬 | 12〜2月 | ❄️ |

---

## 2. お知らせ記事を追加する

`news.html` を開き、`<div class="news-list" id="newsList">` の中に記事ブロックを追加します。新しい記事ほど上に書きます。

```html
<article class="news-card fade-in" data-type="info">
  <div class="news-date">
    <div class="month">May</div>   <!-- 月の英語3文字 -->
    <div class="day">18</div>      <!-- 日 -->
    <div class="year">2026</div>   <!-- 西暦 -->
  </div>
  <div class="news-body">
    <span class="news-tag tag-report">活動報告</span>
    <h3>【5月活動報告】〇〇しました</h3>
    <p>本文をここに書きます。</p>
  </div>
</article>
```

タグの種類と設定値：

| 種別 | `data-type` | タグCSSクラス |
|------|-------------|---------------|
| 開催情報 | `event`  | `tag-event` |
| 活動報告 | `report` | `tag-report` |
| お知らせ | `info`   | `tag-info` |
| 準備中   | `info`   | `tag-notice` |

- ページ上部の「次回開催」バナーは `events.js` の内容が自動反映されるため編集不要です。
- **お知らせ記事の本文は多言語化対象外です。** 新規記事は上記テンプレートのように `data-i18n` を付けず、日本語（または必要な言語）で直接記述してください。
  （初期掲載の3記事だけは `data-i18n` キーで翻訳されていますが、通常の運用では直接記述で構いません。）

---

## 3. ページ内のテキストを変更する（多言語対応）

ナビゲーション・見出し・説明文など、ページ上の主なテキストは **`i18n/<言語>.json`** で言語ごとに管理されています。

対応言語は **7言語**：

| コード | 言語 |
|--------|------|
| ja | 日本語（デフォルト・基準）|
| en | 英語 |
| pt | ポルトガル語 |
| vi | ベトナム語 |
| es | スペイン語 |
| zh | 中国語（簡体字）|
| id | インドネシア語 |

### テキストの構造

各 `i18n/<言語>.json` は同じキーを持つフラットな JSON です。

```json
// i18n/ja.json
{
  "hero-h1": "桃花台で子どもたちのための<br><span>プログラミング・クラブ</span>",
  ...
}
```

同じキーが全7言語ファイルに存在します。**テキストを変更するときは7言語すべての同じキーを更新してください。**

```json
// i18n/ja.json
"hero-h1": "桃花台で子どもたちのための<br><span>プログラミング・クラブ</span>",
// i18n/en.json
"hero-h1": "A Coding Club<br><span>for Kids in Tokadai</span>",
```

### 新しいテキストブロックを HTML に追加するとき

1. HTML 要素に `data-i18n` 属性（HTMLタグを含む場合は `data-i18n-html`）を付けてキー名を指定します。
   ```html
   <p data-i18n="my-new-key">日本語のデフォルトテキスト</p>
   ```
2. `i18n/` の **全7言語ファイル**に同じキーのエントリを追加します。
3. `python3 scripts/check-i18n.py` でエラーなしを確認します。

> HTML 要素の日本語テキストは `i18n/ja.json` の値と**完全一致**させる必要があります（チェックで検証されます）。
> HTML を変更したのに `i18n/*.json` の更新を忘れると、外国語表示で古い内容が残ります。

### HTML ファイル内の固定テキスト

以下は `i18n/*.json` ではなく HTML ファイルに直接書かれています：

- `news.html` … 個別の記事本文（上記 §2 参照）
- `venue.html` … 地図の埋め込み（iframe）

---

## 4. 開催場所の情報を更新する

### 住所

住所は i18n 管理になっています（キー `venue-dd-address`）。変更するときは **全7言語**の `i18n/*.json` の `venue-dd-address` を更新してください。

```json
"venue-dd-address": "愛知県小牧市（桃花台エリア）",
```

### 地図（Google マップ埋め込み）

地図は `venue.html` の `<iframe>` に直接書かれています。差し替え方：

1. Google マップ（maps.google.com）で会場を検索
2. 「共有」→「地図を埋め込む」をクリック
3. 表示された `<iframe>` の `src="..."` の値をコピー
4. `venue.html` の iframe タグの `src` を差し替え

---

## 5. お問い合わせフォームのメール送信

メール送信は **Formspree** で設定済みです（`script.js` の `FORM_ENDPOINT`）。
送信先や受付先を変更したい場合のみ、以下の手順でエンドポイントを差し替えます。

1. https://formspree.io にアクセスしてログイン
2. フォームのエンドポイント URL を取得（例: `https://formspree.io/f/xabcdefg`）
3. `script.js` の次の行を書き換え
   ```js
   const FORM_ENDPOINT = 'https://formspree.io/f/xabcdefg';
   ```

無料プランでは月50件まで受信できます。

---

## 6. 季節の演出について（自動切替）

トップページのヒーロー（大きなビジュアル）は、その日の日付を見て自動的に季節の見た目に変わります。手動操作は不要です。

| 季節 | 月 | 見た目 |
|------|------|------|
| 春 | 3〜5月 | 桃の花・ピンク |
| 夏 | 6〜8月 | 新緑・青空 |
| 秋 | 9〜11月 | 紅葉・給水塔 |
| 冬 | 12〜2月 | 雪・青紺 |

手動でテストしたい場合は `script.js` の次の行を一時的に書き換えると季節を強制できます。

```js
const SEASON = getSeason();
// ↓ 一時的に固定
const SEASON = 'summer';   // 'spring' / 'summer' / 'autumn' / 'winter'
```

確認後は必ず元の行に戻してください。

---

## 7. ファイルの公開（アップロード）について

ファイルを編集したら、ホスティングサービスにアップロードして反映します。
更新したファイルだけアップロードすれば反映されます。

- `events.js` だけ更新した場合 → `events.js` だけでOK
- テキストを変更した場合 → 変更した `i18n/*.json`（と HTML を触ったなら該当 HTML）

アップロード不要なファイル（ローカル作業専用）：

- `scripts/` … 整合性チェックスクリプト
- `.claude/` … Claude Code プロジェクト設定
- `運用方法.md` / `README.md` / `CLAUDE*.md` … ドキュメント

---

## 8. 多言語整合性チェック（Claude Code 使用時は自動）

HTML と `i18n/*.json` の整合性を自動チェックする仕組みが組み込まれています。
Claude Code で `.html` ファイルや `i18n/*.json` を編集すると、編集直後にチェックが走り、問題があれば通知されます。

チェック内容：

1. HTML で使われているキー（`data-i18n` / `data-i18n-html` 属性の値）が、`i18n/` の全7言語ファイルに存在するか
2. HTML の日本語デフォルトテキストと `i18n/ja.json` の値が一致しているか

### 手動でチェックを実行する

```bash
python3 scripts/check-i18n.py
```

問題がなければ：

```
i18n check: OK — all keys present and ja defaults match
```

問題があれば `MISSING`（キー欠落）や `MISMATCH`（テキスト不一致）が表示されます。

### 新しいキーを追加するときの手順（まとめ）

1. HTML に `data-i18n="new-key"` を追加
2. `i18n/ja.json` に `"new-key": "日本語テキスト"` を追加
3. 残り6言語（en / pt / vi / es / zh / id）の `*.json` にも同じキーを追加
4. `python3 scripts/check-i18n.py` でエラーなしを確認

---

## 9. 困ったときの確認ポイント

**ページが真っ白になった**
- HTML ファイルの記述ミス（タグの閉じ忘れなど）の可能性 → 直前の変更を元に戻す

**次回開催情報が更新されない**
- `events.js` の保存・アップロードを確認
- `tbd: false` になっているか確認
- ブラウザのキャッシュをクリア（Ctrl+Shift+R）

**テキストが変わらない / 外国語が日本語のまま**
- 変更した `i18n/*.json` の保存・アップロードを確認
- 該当言語ファイルにキーが存在するか確認
- `python3 scripts/check-i18n.py` を実行して `MISSING` / `MISMATCH` を確認
- ブラウザのキャッシュをクリア（Ctrl+Shift+R）

**地図が表示されない**
- `venue.html` の iframe の `src` URL を確認 → Google マップの「共有」から正しい埋め込み URL を取得して差し替え

**メールが届かない**
- `script.js` の `FORM_ENDPOINT` が正しいか確認
- Formspree のダッシュボードでフォームが有効か確認
- 迷惑メールフォルダを確認
